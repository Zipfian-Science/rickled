MYSQL:
  conn: 127.0.0.1
  usr: test

skilltax:
  type: api_json
  url: https://skilltax-api-nl-rrlwpuonnq-ew.a.run.app/
  expected_http_status: 500

some_field: _|arg_name|_

encoded:
  type: base64
  load: dG9vIG1hbnkgc2VjcmV0cw==

some_reference: &some_reference_ref
  first: 3.14
  second: 42

another_rick:
  type: from_file
  file_path: './tests/placebos/test_config.json'
  load_as_rick: true
  deep: true
  load_lambda: true

a_csv:
  type: from_csv
  file_path: './tests/placebos/test.csv'
  load_as_rick: true
  fieldnames: null

another_text:
  type: from_file
  file_path: './tests/placebos/test_config.json'

BASICS:
  text: test
  dictionary:
    one: value
    two: value
  number: 2
  list:
    - one
    - two
    - four
    - name: John
      age: 20
  USERNAME:
    type: env
    load: USERNAME
  callable:
    type: lambda
    load: "lambda: print('hell world!')"
  datenow:
    type: lambda
    import:
      - "from datetime import datetime as dd"
    load: "lambda: print(dd.utcnow().strftime('%Y-%m-%d'))"
  math_e:
    type: function
    name: test
    args:
      x: 7
      y: null
      s: hello world
      any:
        - 1
        - hello
    import:
      - "math"
    load: >
      def test(x, y, s, any):
        print(math.e)
        iii = 111
        print(iii)
        print(x,s)
        if y:
          print(type(y))
        else:
          print(y)
        for i in any:
          print(i)
